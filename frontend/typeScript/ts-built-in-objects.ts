// ECMAScript 的内置对象
// Boolean、Error、Date、RegExp 等。
let bool: Boolean = new Boolean(1);

let e: Error = new Error();

let date: Date = new Date();
//date.getFullYear()

let r: RegExp = /[a-z]/;

//DOM 和 BOM 的内置对象
let bodys: Element = document.body;
